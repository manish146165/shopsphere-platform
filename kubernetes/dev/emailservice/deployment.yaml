apiVersion: apps/v1
kind: Deployment

metadata:
  name: emailservice
  namespace: shopsphere

  labels:
    app: emailservice
    app.kubernetes.io/name: emailservice
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: shopsphere
    app.kubernetes.io/managed-by: kubectl

spec:

  replicas: 2

  revisionHistoryLimit: 5

  strategy:
    type: RollingUpdate

    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1

  selector:
    matchLabels:
      app: emailservice

  template:

    metadata:

      labels:
        app: emailservice
        app.kubernetes.io/name: emailservice
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: shopsphere

    spec:

      terminationGracePeriodSeconds: 30

      containers:

      - name: emailservice

        image: 221237748109.dkr.ecr.ap-south-1.amazonaws.com/shopsphere:emailservice-v1

        imagePullPolicy: Always

        ports:

        - name: http
          containerPort: 8080

        env:

        - name: PORT
          value: "8080"

        - name: DISABLE_PROFILER
          value: "1"

        - name: DISABLE_TRACING
          value: "1"

        - name: DISABLE_STATS
          value: "1"

        - name: OTEL_SERVICE_NAME
          value: "emailservice"

        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector:4317"

        - name: OTEL_EXPORTER_OTLP_PROTOCOL
          value: "grpc"

        - name: OTEL_RESOURCE_ATTRIBUTES
          value: "service.name=emailservice,deployment.environment=dev"

        resources:

          requests:
            cpu: 100m
            memory: 128Mi

          limits:
            cpu: 250m
            memory: 256Mi

        readinessProbe:

          tcpSocket:
            port: 8080

          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

        livenessProbe:

          tcpSocket:
            port: 8080

          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
